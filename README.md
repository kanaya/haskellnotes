# Haskellのノート

「Haskellのノート」はプログラミング言語Haskellの入門書である．

## バージョン番号とブランチの説明

### バージョン番号

バージョン番号は `x.y.z` とし `x` は大幅な書き換え，追記があった場合に更新する．小幅な書き換え，追記があった場合は `y` を更新する．エラーの修正があった場合は `z` を更新する．

### ブランチ

`main` ブランチに最新のリリースを置く．`release-x.y` ブランチにバージョン `x.y.*` のリリース準備版を置く．